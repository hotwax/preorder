import type { Root } from "yaml-unist-parser";
import type { YAMLProgram } from "./ast";
/**
 * Convert yaml-unist-parser root to YAMLProgram
 */
export declare function convertRoot(node: Root, code: string): YAMLProgram;
