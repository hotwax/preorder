import { Context } from "../transform";
import { Comment, Content } from "../types";
import * as YAML from "../yaml";
export declare function transformContent(node: YAML.ast.Node, context: Context, isNotMiddleComment?: (comment: Comment) => boolean): Content;
